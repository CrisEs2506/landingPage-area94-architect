---
import { Image } from "astro:assets";
import nosotrosImg from "../assets/Nosotros.png";
import identidadImg from "../assets/Identidad.png";
import desarrolloImg from "../assets/Desarrollo.png";
import estrategiaImg from "../assets/Estrategia.png";
---

<section class="nosotros-section" id="nosotros">
  <div class="nosotros-container">
    <!-- Left Column -->
    <div class="nosotros-content">
      <h2 class="title">Nosotros</h2>
      <p class="subtitle">
        <strong>Área 94 (A94)</strong> impulsa inversiones inmobiliarias con más de
        diez años de experiencia en proyectos comerciales y residenciales, integrando
        Arquitectura y Gestión Legal estratégica integral.
      </p>

      <Image
        src={nosotrosImg}
        alt="Estructura de casa transparente, diseño arquitectónico"
        class="main-img"
      />
    </div>

    <!-- Right Column: Cards -->
    <div class="cards-column">
      <!-- Card 1 -->
      <div class="info-card">
        <div class="card-img-wrapper">
          <Image src={identidadImg} alt="Identidad" class="card-img" />
        </div>
        <div class="card-text">
          <h3 class="card-title">Identidad</h3>
          <p class="card-desc">
            En Área 94 convertimos proyectos inmobiliarios en inversiones mejor
            estructuradas, más seguras y con mayor potencial de valorización.
          </p>
        </div>
      </div>

      <!-- Card 2 -->
      <div class="info-card">
        <div class="card-img-wrapper">
          <Image src={desarrolloImg} alt="Desarrollo" class="card-img" />
        </div>
        <div class="card-text">
          <h3 class="card-title">Desarrollo</h3>
          <p class="card-desc">
            Integramos suelo, diseño, licenciamiento y obra para transformar
            oportunidades en activos viables, rentables y comercializables.
          </p>
        </div>
      </div>

      <!-- Card 3 -->
      <div class="info-card">
        <div class="card-img-wrapper">
          <Image src={estrategiaImg} alt="Estrategia" class="card-img" />
        </div>
        <div class="card-text">
          <h3 class="card-title">Estrategia</h3>
          <p class="card-desc">
            Analizamos cada proyecto con visión técnica, legal y comercial para
            proteger la inversión y potenciar su retorno.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .nosotros-section {
    padding: 26px 16px;
    background-color: var(--bg); /* Body white */
    display: flex;
    justify-content: center;
  }

  .nosotros-container {
    max-width: 1600px;
    width: 100%;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr; /* Match Hero proportions */
    gap: 44px;
    align-items: stretch; /* Cambio: stretch para que la columna izquierda tenga el mismo alto que las tarjetas */
  }

  /* Left Column */
  .nosotros-content {
    display: flex;
    flex-direction: column;
    gap: 24px;
    height: 100%;
    min-height: 0; /* Permite a los hijos flex contraerse más allá de su tamaño intrínseco */
  }

  .title {
    padding-left: 6px;
    font-size: clamp(32px, 3.5vw, 44px);
    font-weight: 600;
    color: var(--primary);
    margin: 0;
    line-height: 1.15;
    letter-spacing: -0.5px;
  }

  .subtitle {
    padding-left: 6px;
    font-size: 15px;
    line-height: 1.6;
    color: var(--secondary);
    margin: 0;
    max-width: 100%;
  }

  .subtitle strong {
    color: var(--primary);
  }

  .main-img {
    margin-top: auto; /* Empuja la imagen hacia el final (abajo) para alinearse con la última tarjeta */
    width: 100%;
    flex: 1; /* Permite que tome el espacio sobrante sin forzarse a crecer */
    min-height: 0; /* Crucial: evita que el tamaño intrínseco de la imagen rompa la columna */
    max-height: 350px; /* Limita el alto asegurando que no exceda el alto total impuesto por las tarjetas */
    object-fit: contain; /* Ajusta la imagen completa en el recuadro sin recortar */
    /*object-position: left bottom;*/ /* Ancla la imagen abajo a la izquierda */
    mix-blend-mode: normal; /* Es un PNG transparente, no necesita blend mode */
  }

  /* Right Column */
  .cards-column {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .info-card {
    background-color: var(--primary); /* Black, same as primary button */
    border-radius: 24px;
    display: flex;
    align-items: stretch;
    padding: 8px; /* Internal padding to create the small image border effect */
    height: 160px;
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  .info-card:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 16px 32px rgba(35, 39, 47, 0.2);
  }

  .card-img-wrapper {
    width: 150px;
    height: 100%;
    flex-shrink: 0;
  }

  .card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 16px; /* Slightly less than the container to look nested perfectly */
  }

  .card-text {
    padding: 16px 24px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    flex: 1;
  }

  .card-title {
    font-size: 16px;
    font-weight: 600;
    margin: 0 0 8px 0;
    color: var(--accent); /* White */
  }

  .card-desc {
    font-size: 13px;
    line-height: 1.5;
    color: rgba(
      255,
      255,
      255,
      0.75
    ); /* Soft white/gray for better readability on black */
    margin: 0;
  }

  /* Responsive Design */
  @media (max-width: 1199px) {
    /* Tablet */
    .nosotros-section {
      padding: 26px 16px;
      padding-top: 5px;
      padding-bottom: 16px;
    }
    .nosotros-container {
      grid-template-columns: 1fr;
      gap: 30px;
    }
    .nosotros-content {
      height: auto;
      gap: 16px;
    }
    .title {
      font-size: clamp(38px, 5vw, 42px);
    }
    .main-img {
      max-height: none;
      height: auto;
      width: 75%;
      align-self: center;
      flex: none; /* En tablet vuelve a su comportamiento normal */
      object-fit: initial;
      object-position: initial;
    }
    .cards-column {
      display: grid;
      grid-template-columns: 1fr;
      gap: 16px;
    }
  }

  @media (max-width: 767px) {
    /* Mobile */
    .nosotros-section {
      padding: 24px 16px;
      padding-top: 5px;
    }
    .nosotros-container {
      gap: 32px;
    }
    .nosotros-content {
      height: auto;
      gap: 16px;
    }
    .title {
      font-size: 32px;
    }
    .subtitle {
      font-size: 14px;
      line-height: 1.5;
    }
    .main-img {
      max-height: none;
      height: auto;
      width: 75%;
      align-self: center;
      flex: none;
      border-radius: 20px;
      object-fit: initial;
      object-position: initial;
    }
    .cards-column {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 24px;
    }
    .info-card {
      flex-direction: column;
      height: auto;
      width: 100%;
      max-width: 380px;
      padding: 12px;
    }
    .card-img-wrapper {
      width: 100%;
      height: 140px; /* Reduced height to make images smaller */
    }
    .card-img {
      border-radius: 16px;
    }
    .card-text {
      padding: 16px;
    }
    .card-title {
      font-size: 18px;
      margin-bottom: 6px;
    }
  }
</style>
